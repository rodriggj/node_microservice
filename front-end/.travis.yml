sudo: required
language: generic
services: 
  - docker

before_install:
  - docker build -t rodriggj/frontend:1.4 -f Dockerfile.dev .

script: 
  - docker run -e CI=true rodriggj/frontend:1.4 npm run test